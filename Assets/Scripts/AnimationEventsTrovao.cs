using UnityEngine;

public class AnimationEventsTrovao : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void DestruirSkill()
    {
        Destroy(gameObject);
    }
}
